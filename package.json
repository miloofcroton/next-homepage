{
  "scripts": {
    "test": "exit 0",
    "prebuild": "npm run test",
    "build": "next build && next export -o dist",
    "now-build": "echo 'Built on Travis CI'"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
